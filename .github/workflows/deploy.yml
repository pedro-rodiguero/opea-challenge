# Workflow to deploy the app to GitHub Pages
name: Deploy static content to Pages

on:
  # Execute when code is pushed to main branch
  push:
    branches: ["main"]

  # Allow manual deployment from the Actions tab
  workflow_dispatch:

# Define permissions for the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Avoid multiple concurrent deployments
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: companies-app/package-lock.json
      - name: Install dependencies
        run: npm ci
        working-directory: ./companies-app
      - name: Build
        run: npm run build
        working-directory: ./companies-app
        env:
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
      - name: Copy index.html to 404.html
        run: cp companies-app/dist/index.html companies-app/dist/404.html
        shell: bash
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload from the correct build directory
          path: "./companies-app/dist"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
